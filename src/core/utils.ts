export const enum Header {
  API_KEY = 'x-api-key',
  AUTHORIZATION = 'authorization',
}

// Departments = ['USER', 'ORDER'];

// Actions = ['CREATE', 'UPDATE', 'READ', 'DELETE', 'APPROVE', 'REJECT'];
